import { Injectable } from '@nestjs/common';

@Injectable()
export class UsersService {
  // TODO: Implémenter le CRUD
  // create(dto: CreateUserDto) {}
  // findAll() {}
  // findOne(id: string) {}
  // update(id: string, dto: UpdateUserDto) {}
  // remove(id: string) {}
}
